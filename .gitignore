node_modules/
dist/
dev-dist/
*.tsbuildinfo
.DS_Store
*.log
.env
.env.local
.wrangler/
app-audit/
# `npm pack` / `bun pm pack` output — the publish tarball is built in CI, never committed.
*.tgz
# scripts/bigvault probe deps are a private install (playwright) — no lockfile churn.
scripts/bigvault/bun.lock
